spring.application.name=attendance

# --- Railway MySQL (PUBLIC proxy; good for local dev and anywhere outside Railway) ---
spring.datasource.url=jdbc:mysql://turntable.proxy.rlwy.net:11941/railway?serverTimezone=UTC&allowPublicKeyRetrieval=true&sslMode=REQUIRED
spring.datasource.username=root
spring.datasource.password=GbpBTMRjRidqUhubjtSPvTyMdjwgpHnj

# --- JPA / Hibernate ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# --- Optional: Hikari tweaks (safe defaults) ---
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.minimum-idle=1

# --- Server port ---
# Use Railway?s injected PORT if present; otherwise run on 8080 locally
server.port=${PORT:8080}



# If JPA jars are still in your build (or coming transitively), hard-disable them:
# Allow larger uploads (tune as you like)
spring.servlet.multipart.max-file-size=20MB
spring.servlet.multipart.max-request-size=20MB

# (Usually not needed, but harmless if you set it)
server.tomcat.max-swallow-size=50MB

face.api.key=s17AGO3iWNfbdI9QkkyIiS6daUW8lk0K
face.api.secret=gZMIiSh3YwJvU3d29-SSU-Uw0KdPiXSV
face.faceset.outerId=employee_faces
# tune if needed (FaceService uses this if you added the property)
face.threshold=80.0

spring.jpa.properties.hibernate.globally_quoted_identifiers=true
